
= image_tag @promotion.user.avatar.thumb.url
%h2= @promotion.user.name



%h2= @promotion.pitch
%h2= @promotion.description
%h2= @promotion.price
%h2= @promotion.available_on_date
%h2= @promotion.start_time
%h2= @promotion.end_time
%h2= @promotion.cuisine
%h2= @promotion.hotness

-if current_user
  - if current_user.id == @promotion.user.id
    =link_to "Edit", edit_promotion_path(@promotion)
    =link_to "Delete", promotion_path(@promotion), method: :delete

#map-container
  #map-canvas

#address
  %h3= @promotion.user.address
  %h3= @promotion.user.city
  %h3= @promotion.user.postcode

=render "charges/stripe_form", promotion: @promotion 






